# River Vector Autonomy Suite Requirements
# Note: ROS2 Humble (rclpy) is expected to be installed via system packages.

numpy>=1.24.0
pyserial>=3.5
meshtastic>=2.3.0       # LoRa backup beacon — optional, falls back to sim mode
pypubsub>=4.0.3         # Required by meshtastic for message callbacks
opencv-python>=4.7.0
pyyaml>=6.0
requests>=2.28.0
pytest>=7.3.0
jsonschema>=4.17.0
